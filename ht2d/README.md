# ht2d — Hypothesis Test Drift Detection

A minimal, modular Python package for hypothesis-test-based concept drift detection.

## Install (dev)

```bash
python -m venv .venv && source .venv/bin/activate
pip install -e .[dev,river]
```
